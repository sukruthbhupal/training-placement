services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: smile_db
      POSTGRES_USER: smile_user
      POSTGRES_PASSWORD: smile_pass
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
